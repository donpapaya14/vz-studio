# Mini-SPEC — v3.1 "más wow" + 2 bugs (2026-09-03)

Feedback de Vladys tras el deploy v3. Nivel N2 (js/lib tiene tests; fx se verifica con capturas). Coste 0. Sin cambiar copy ni estructura de secciones.

| ID | Criterio | Verificación exacta |
|---|---|---|
| B-1 | Hover de `.svc-item` (Servicios) legible: fondo claro `--ink`, texto `--humo`, h3 y p heredan | `grep -n "svc-item:hover" css/style.css` muestra `color:var(--humo)`; captura con hover sobre "Webs a medida" |
| B-2 | Hero en móvil real muestra el vídeo dentro de las letras VZ y se mueve: ruta móvil sin WebGL = `<video>` visible + máscara SVG con texto "VZ"; `video.play()` explícito tras asignar `src`, en `canplay` y en primer `touchstart` | `grep -c "play()" js/hero.js` ≥ 2; `grep -n "<mask" index.html en/index.html`; captura a 606px con el vídeo visible dentro de las letras; en iPhone real lo comprueba Vladys |
| C-1 | Antes/después con "cortina": en cada `.case` las dos capturas se apilan en un solo escenario 16:9, el DESPUÉS se revela de izquierda a derecha con `clip-path` al hacer scroll (ScrollTrigger scrub) y sigue al cursor/dedo en hover/arrastre; línea divisoria verde de 2px con etiqueta mono ANTES / DESPUÉS a cada lado | `ls js/fx/cortina.js`; `grep -c 'class="cortina' index.html` = 5; captura a mitad de cortina; GIF de 4 s de scroll por Rediseño |
| C-2 | Efecto "hoja": al bajar, la sección siguiente sube tapando a la actual, que queda fija por abajo (`position:sticky; top:calc(100vh - alto)` calculado en JS), se encoge a `scale(.96)` y se oscurece `brightness(.55)` mientras la tapan; la que sube lleva `border-radius:28px 28px 0 0` y sombra. Se aplica a todas las secciones salvo `#trabajo`/`#work` (pin del raíl) | `ls js/fx/hoja.js`; `grep -c "data-hoja" index.html` ≥ 6; GIF de 6 s bajando de Hero a Rediseño; `prefers-reduced-motion` → sin sticky ni escala |
| C-3 | Nada de lo anterior rompe lo existente: consola limpia, Lighthouse móvil Perf ≥ 90, detector PASA, vitest verde, `/en/` espejo | comandos de `SPEC.md` C-8/C-10/C-11/C-12 |

**Fuera de alcance:** copy, nuevas secciones, three.js, cambios de paleta.
**Amenazas:** ninguna nueva (sin datos, sin backend, CSP intacta: cero inline; `element.style.top` por CSSOM está permitido).
