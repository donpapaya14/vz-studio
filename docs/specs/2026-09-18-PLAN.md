# PLAN — SEO: casos, guías, diagnóstico y publicación automática

SPEC: `2026-09-18-seo-casos-guias-diagnostico.md`. Orden pensado para que cada paso
deje el sitio en estado desplegable: si la noche se corta a la mitad, lo hecho sirve.

| # | Paso | Cierra | Verificación |
|---|---|---|---|
| 0 | Commitear el WIP de v3.1 que ya estaba en `master` (idéntico a `fix/hero-movil`) para no mezclarlo con lo nuevo | P-1 | `git log -1` y `git status` limpio antes de empezar |
| 1 | `js/lib/marcado.js` — parser md→HTML con TDD (rojo → verde) | C-7 | `npx vitest run js/lib/marcado.test.js` |
| 2 | `js/lib/diagnostico.js` — calculadora + generador de enlace, TDD + property-based | C-8, C-9 | `npx vitest run js/lib/diagnostico.test.js` |
| 3 | `js/lib/plantilla.js` — función pura que arma el HTML de una página, con tests | C-3, C-4 | `npx vitest run js/lib/plantilla.test.js` |
| 4 | `bin/construir.mjs` — lee `contenido/**/*.md`, escribe HTML e índices, regenera `sitemap.xml` | C-3, C-4, C-5, C-6 | `node bin/construir.mjs` y contar ficheros |
| 5 | `bin/verificar.mjs` — auditor propio: h1 único, canonical, description, JSON-LD, enlaces internos, nº de entradas en índices | C-3, C-4, C-5, C-6 | `node bin/verificar.mjs` → 0 errores |
| 6 | Contenido: 5 casos con material real del repo (`assets/*-old` / `*-new`) | C-3 | `ls contenido/casos/*.md` = 5 |
| 7 | Contenido: 8 guías apuntando a búsquedas reales de su cliente objetivo | C-4 | `ls contenido/guias/*.md` = 8 |
| 8 | Página `/diagnostico/` + `js/fx/diagnostico.js` (pegamento DOM) | C-9 | Captura del flujo y del enlace generado |
| 9 | CSS: `.prosa`, `.listado`, `.diag` reutilizando tokens; enlaces en nav y footer | P-1 | Diff de `css/style.css` |
| 10 | Analítica + `connect-src 'self'` en `vercel.json` | C-1, C-2 | `grep` de ambos |
| 11 | `bin/publicar-guia.sh` + `bin/lib_red.sh` + plist launchd (rama, nunca master) | C-10, C-11 | `DRY_RUN=1` y `plutil -lint` |
| 12 | Verificación final y push | C-12 | vitest, detector, PageSpeed, `git push` |

**Fuera de alcance del plan**: chatbot, traducción de casos/guías al inglés, CMS.

**Riesgo asumido**: el paso 0 sube a producción el trabajo v3.1 (cortina, hoja, hero
móvil) que Vladys tenía sin commitear. Está cubierto por su propio mini-SPEC del
2026-09-03 y verificado en local antes de empujar.
